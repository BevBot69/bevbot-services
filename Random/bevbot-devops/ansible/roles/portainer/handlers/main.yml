---
# ansible/roles/portainer/handlers/main.yml
- name: Reload and restart portainer
  ansible.builtin.systemd:
    name: portainer.service
    state: restarted
    daemon_reload: true
    scope: user
  become: true
  become_user: "{{ podman_user }}"
