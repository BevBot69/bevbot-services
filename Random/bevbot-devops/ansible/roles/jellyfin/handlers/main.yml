---
# ansible/roles/jellyfin/handlers/main.yml
- name: Reload and restart jellyfin
  ansible.builtin.systemd:
    name: jellyfin.service
    state: restarted
    daemon_reload: true
    scope: user
  become: true
  become_user: "{{ podman_user }}"
